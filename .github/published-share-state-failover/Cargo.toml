[package]
name = "kafrust-published-share-state-failover"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
kafrust = "__KAFRUST_VERSION__"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
