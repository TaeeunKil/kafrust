[package]
name = "kafrust-published-config-resources-smoke"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
kafrust = { version = "__KAFRUST_VERSION__" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
