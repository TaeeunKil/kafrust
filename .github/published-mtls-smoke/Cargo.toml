[package]
name = "kafrust-published-mtls-smoke"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
kafrust = { version = "__KAFRUST_VERSION__", features = ["tls"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
